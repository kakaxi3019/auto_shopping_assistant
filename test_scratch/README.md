# Auto Shopping Assistant - Test Scratchpad

这个目录用于存放测试代码以验证系统的各项功能。在完成测试后，该目录可以被安全地整体删除。

## 运行测试
您可以使用以下命令在此目录下执行独立的脚本以验证具体逻辑：
```bash
npx ts-node test_scratch/run_test.ts
```
或者通过 vitest 执行此目录下的测试：
```bash
npx vitest run test_scratch/
```
